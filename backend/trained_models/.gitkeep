# This file keeps the trained_models/ directory tracked by git.
# Actual .pkl and .h5 model binaries are gitignored (too large for git).
# After training, place your model files here and update config.py paths.
